# KnifeProject

This project was generated with [Angular CLI](https://github.com/angular/angular-cli) version 15.1.6.


## Build

Запустить `ng build --configuration release` Для сборки проекта. Собранный проект хранится в `dist/` директории.

## Run

Для запуска собранного проекта `angular-http-server -p {port}`.
